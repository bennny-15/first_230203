
public class ReferenceDateType {

	public static void main(String[] args) {
		// 참조형 타입 = 기본형 타입을 제외한 모든 것(배열, 클래스 등)
		
		// 기본형 타입
		int i = 4;

		
		// String 클래스를 이용해서 참조형 타입 변수 str 선언
		String str = new String("hello");
		// new = 클래스를 메모리에 올려달라, 메모리에 올라간 클래스 = 인스턴스, 메모리에 올라간 인스턴스를 참조(레퍼런스)하는 변수 = 참조변수(str)
		// str 변수 안에는 메모리의 위치값이 저장되는 것 == string 인스턴스를 참조한다

	}

}